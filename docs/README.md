# relearning_c

오랜만에 C 다시 공부하는 재활용 저장소.

## 구조

- `01_basics` ~ `11_projects` : 주제별 연습 폴더
- `customlib` : 표준 라이브러리 함수 직접 구현 연습 (libft 재개발 대비)
- `Makefile` : `make run FILE=경로/main.c` 로 컴파일+실행

## 빌드

```bash
gcc -Wall -Wextra -Werror <파일들> -o <output>
```
